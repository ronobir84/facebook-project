 
const CardItem = ({ item }) => {
    const { _id, profile, name, Date_time, text, image } = item;
    console.log(item.image)
    return (
        <div>
            <div>
                
            </div>
        </div>
    );
};

export default CardItem;